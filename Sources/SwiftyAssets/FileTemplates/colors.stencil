/*
    SwiftyColors.swift
    {{ projectName }}

    Generated by SwiftyAssets 🦊 on {{ date }}
    Copyright © 2020, {{ projectName }}. All rights reserved.
*/

#if canImport(UIKit)
import UIKit

// swiftlint:disable force_unwrapping
extension SwiftyAssets {
    class UIColors {
        {% for color in colors %}
        static var {{ color.name }}: UIColor {
            return UIColor(named: "{{ color.name }}")!
        }
        {% endfor %}
    }
}
// swiftlint:enable force_unwrapping
#endif

#if canImport(SwiftUI)
import SwiftUI

extension SwiftyAssets {
    class Colors {
        {% for color in colors %}
        static var {{ color.name }}: Color {
            return Color("{{ color.name }}")
        }
        {% endfor %}
    }
}
#endif
